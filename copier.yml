_subdirectory: template
with_hugo:
  type: bool
  default: true
_exclude:
  - "{% if not with_hugo %}hugo-tasks{% endif %}"
  - "{% if not with_hugo %}.mise/tasks/hugo{% endif %}"
  - "{% if not with_hugo %}.mise/conf.d/10-hugo.toml{% endif %}"
