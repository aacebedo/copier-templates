---
# Merged over .updatecli/values.yaml by the update-deps task.
miseDeps:
  rust:
    - slug: rust
      answerKey: rust_version
      tomlKey: rust
      kind: githubrelease
      owner: rust-lang
      repo: rust
      trimPrefix: false
      file: .mise/conf.d/10-rust.toml
